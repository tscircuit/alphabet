{
  "name": "alphabet",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsup-node ./index.ts --format esm --dts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tsup": "^8.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
