{
  "name": "@tscircuit/alphabet",
  "version": "0.0.6",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@biomejs/biome": "1.7.3",
    "@flatten-js/core": "^1.6.6",
    "@types/bun": "latest",
    "@types/opentype.js": "^1.3.8",
    "bun-match-svg": "^0.0.11",
    "opentype.js": "^1.3.4",
    "tsup": "^8.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsup-node ./index.ts --format esm --dts && bun run build:font",
    "build:font": "bun scripts/build-font.ts",
    "format": "biome format --write ."
  },
  "type": "module"
}
